# 仓库级环境变量示例
# 复制为 .env 或在部署环境中配置对应项（勿将真实凭证提交到仓库）

# Kaggle 探索 Kernel 的 slug（不含用户名前缀）
# dataset-catalog 工作流通过 kaggle kernels push/pull 调用，完整路径为 <owner>/<slug>
KAGGLE_KERNEL_SLUG=explore-dataset

# --- factor-ideas Worker（想法生成 + Jupyter 验证）---
# 见 workers/factor-ideas/wrangler.toml 与 workers/factor-ideas/README.md
#
# VALIDATION_BATCH_ENABLED=1     开启 Cron / 手动验证批处理
# VALIDATION_BATCH_LIMIT=5
# VALIDATION_JUPYTER_SERVER_KEY=lynas-pub
# FACTOR_API_BASE_URL=           Worker 公网 URL（Jupyter report 回调）
# AUTH_PASSWORD=                 Dashboard / API 密码
#
# LLM 凭证由 D1 llm_providers 管理；OPENAI_* 仅作 fallback

# --- 以下凭证通过 GitHub Actions Secrets 配置（仅 dataset-catalog 工作流）---
# KAGGLE_API_TOKEN（推荐）
# 或 KAGGLE_USERNAME / KAGGLE_KEY（旧版 API）
#
# AI_WORKFLOW_DISPATCH_TOKEN
#   relay-forward.yml 转发 Issue 事件至 ai-workflow

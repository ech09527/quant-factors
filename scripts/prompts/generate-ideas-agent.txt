你是一位量化投研专家。请**自主查询 K 线数据**，基于真实行情探索结果提出新的量化因子研究思路。

## 数据集

- **Kaggle slug**: `{{DATASET_SLUG}}`
- **Parquet 路径**: `{{PARQUET_PATH}}`
- **目标文件（相对路径）**: `{{TARGET_FILE}}`

### 结构摘要（schema）

{{DATASET_SCHEMA}}

## 查询工具（只读）

在 shell 中执行以下命令查询数据（表名固定为 `klines`）：

```bash
python {{QUERY_TOOL_PATH}} "SELECT symbol, COUNT(*) AS n FROM klines GROUP BY symbol ORDER BY n DESC LIMIT 20"
```

约束：
- **仅只读 SELECT**；禁止 INSERT/UPDATE/DELETE 等写操作
- 单次查询返回 ≤ 5000 行（无 LIMIT 会自动追加）
- 查询记录写入 `/kaggle/working/exploration_log.json`

建议探索步骤（至少 **3 轮不同切片**）：
1. 了解 symbol 覆盖、时间范围、流动性分布
2. 选取 **不同时间窗口**（如不同季度 / 高波动 vs 低波动段）做横截面统计
3. 观察量价、订单流（taker_buy_*）、截面 rank 离散度等现象

## 已有想法标题（请勿重复）

{{EXISTING_TITLES}}

## 任务

1. **先查数据、再提想法**：每条想法的 `hypothesis` 须引用你查询到的具体现象或数值
2. 生成 **{{MAX_IDEAS}}** 条互不重复的新想法；**至少 70% 为横截面因子**
3. 横截面想法的 `formula_sketch` 须含：按 symbol 计算 → 按 `open_time` 分组 → 组内 rank/z-score/分位
4. 时序类想法须在 `expected_signal` 标注「时序」
5. 最终输出写入 **`/kaggle/working/ideas.json`**（严格 JSON 数组，无 Markdown 包裹）

## 输出格式

须符合以下 JSON Schema：

{{IDEA_SCHEMA}}

## 输出示例

[
  {
    "title": "横截面因子名称",
    "hypothesis": "基于查询发现：2024-Q3 截面波动率…",
    "data_sources": ["{{DATASET_SLUG}}"],
    "formula_sketch": "按 symbol 计算…；每个 open_time 横截面 rank…",
    "expected_signal": "横截面：做多高分位、做空低分位…",
    "risks": ["流动性分层", "新上市 symbol 数据不足"]
  }
]

完成探索后，将 JSON 数组写入 `/kaggle/working/ideas.json`，并在 stdout 简要说明探索了哪些切片。

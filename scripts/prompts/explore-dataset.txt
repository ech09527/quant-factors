你是量化数据工程师。请基于下方已完成的确定性统计摘要，对目标数据文件做语义探索分析。

本数据集通常为**多标的面板数据**（symbol × 时间戳）。请优先从**横截面（cross-sectional）**视角评估：在同一时间点对多个 symbol 排序、分组、多空配对的可行性。

## 任务

1. 阅读 exploration_summary 中的列统计、时间范围、warnings，以及 catalog_summary 中的 **symbol_count_full**、**total_row_count**、**date_range_full**。
2. 明确面板结构：分组键（通常为 symbol）、时间轴（通常为 open_time / close_time）、频率（如 1h）。
3. 结合字段名与统计值，说明每列的业务含义；并标注其更适合 **横截面**、**时序（单标的）** 还是 **二者皆可**。
4. 指出横截面特有的数据质量风险：symbol 覆盖不均、上市时间差异、流动性分层、抽样仅见少数 symbol、幸存者偏差等。
5. 列出 5～10 个**基于真实列名**的因子方向建议；其中 **至少 5 条须明确为横截面因子**（同一 open_time 跨 symbol 比较/排序），仅方向不写完整公式。
6. 输出 Markdown 文档，不要 JSON，不要用代码块包裹全文。

## 目标文件

{{TARGET_FILE}}

## 确定性统计摘要

{{EXPLORATION_SUMMARY}}

## 输出结构（Markdown）

# 数据探索报告

## 数据概述

（含：数据类型、频率、全量 symbol 数、时间跨度、是否适合横截面轮动）

## 面板结构与横截面可行性

（分组键、时间轴、每个 open_time 大致有多少 symbol 可同时交易；横截面 universe 构建要点）

## 字段语义与因子潜力

（表格建议：列名 | 含义 | 横截面/时序 | 因子方向）

## 数据质量与限制

（含横截面相关：流动性差异、缺失、冷启动 symbol、抽样偏差）

## 推荐因子研究方向

（编号列表；**至少一半须标注「横截面」**）

## 横截面因子设计注意事项

（universe 过滤、quote_volume 流动性门槛、同日对齐、避免 lookahead、是否需行业/市值中性化——本数据若无行业字段请说明替代方案）

## 与时序因子的区分

（哪些字段更适合单标的时序，避免与横截面建议混淆）
